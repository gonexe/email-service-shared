{
  "name": "shared",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node -r tsconfig-paths/register dist/server.js",
    "clean": "rm -rf ./dist"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ]
}